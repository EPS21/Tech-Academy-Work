@model IEnumerable<ACME.Models.Table>

@{
    ViewBag.Title = ViewBag.qwerasdfzxcv;
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", "Tables")
    <br/>
    @Html.ActionLink("Create V3", "Create", new RouteValueDictionary(new { locale = "en-US", year = DateTime.Now.Year.ToString()}))

    @{ 
        var myDictionary = new Dictionary<string, object>();
        myDictionary.Add("First", "a");
        myDictionary.Add("Second", "b");
        myDictionary.Add("muh%20dick", "c");
    }

    @Html.ActionLink("Create V4", "Create", new RouteValueDictionary(myDictionary))

</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductId, blahblahblah = 333 }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductId })
        </td>
    </tr>
}

</table>
